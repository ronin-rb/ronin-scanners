name: ronin-scanners
summary:
  A Ruby library for Ronin that provides Ruby interfaces to various
  third-party security scanners.

description:
  Ronin Scanners is a Ruby library for Ronin that provides Ruby interfaces
  to various third-party security scanners.

license: GPL-2
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: http://github.com/ronin-ruby/ronin-scanners
has_yard: true

dependencies:
  open_namespace: ~> 0.3
  rprogram: ~> 0.3
  # Port Scanner dependencies
  ruby-nmap: ~> 0.5
  # Credential Scanner dependencies
  net-ssh: ~> 2.1
  # Web dependencies
  nokogiri: ~> 1.4
  net-http-persistent: ~> 1.8
  spidr: ~> 0.3
  # Ronin dependencies:
  # ronin-support: ~> 0.4
  # ronin: ~> 1.4

development_dependencies:
  bundler: ~> 1.0.10
  yard: ~> 0.7.0
  yard-parameters: ~> 0.1.0
